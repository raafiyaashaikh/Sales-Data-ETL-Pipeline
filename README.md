# Sales-Data-ETL-Pipeline
Built an end-to-end ETL pipeline to process sales data from CSV files using Python and SQL. The pipeline extracts raw data, performs data cleaning and transformations, and loads processed data into a relational database for analytical querying.
